
function createRegistrant(name, age, permit = false) {
  return {
    name: name,
    age: age,
    permit: permit
    test
  }
}

module.exports = {
  createRegistrant
  // earnPermit,
  // takeLicenseExam
}